// Environment variables with type safety
export const env = {
  // // API
  // apiUrl: process.env.NEXT_PUBLIC_API_URL || "https://api.elela.vn",
  // assetUrl: process.env.NEXT_PUBLIC_ASSET_URL || "https://assets.elela.vn",

  // // Auth
  // authCookieName: process.env.AUTH_COOKIE_NAME || "auth-token",

  // // App
  // appName: process.env.NEXT_PUBLIC_APP_NAME || "Elena Pharmacy",
  // appUrl: process.env.NEXT_PUBLIC_APP_URL || "https://elela.vn",

  // // Environment
  // nodeEnv: process.env.NODE_ENV || "development",
  // isProduction: process.env.NODE_ENV === "production",
  // isDevelopment: process.env.NODE_ENV === "development",
}

